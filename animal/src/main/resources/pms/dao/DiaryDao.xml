<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="pms.dao.DiaryDao">
	
  <resultMap type="diary" id="diaryMap">
    <id 	column="SCH_NUM" 		property="dno"/>
	<result column="PET_NUM" 		property="pno"/>    
    <result column="START_DATE" 	property="startDate"/>
    <result column="END_DATE" 		property="endDate"/>
	<result column="SCH_CRE" 		property="createDate"/>
	<result column="SCH_CONTENT" 	property="content"/>
	<result column="SCH_TITLE" 		property="title"/>
	<result column="CATE_CODE" 		property="cateCode"/>
	<result column="NOTI_HIDE" 		property="dhide"/>
	<result column="NOTI_VIEWS" 	property="view"/>
	<result column="NOTI_LIKES" 	property="like"/>	
 </resultMap>

  <select id="eventList" resultMap="diaryMap">
    select * from schedule
  </select>
  
  <select id="selectList" resultMap="diaryMap" parameterMap="map">
    select
  		dno, pno, createDate, title, content, cateCode, view, like 
  	from schedule 
  	where cateCode=#{cateCode} and dhide='true'
  	order by dno
    <choose>
      <when test="align == 'desc'">desc</when>
      <otherwise>desc</otherwise>
    </choose>
    limit #{startIndex}, #{length}
  </select>
  
  <select id="petList" resultMap="diaryMap" parameterType="int">
  	select
  		p1.pet_num pno,
  		p1.pet_name pName, 
  		p1.pet_type pType,
  		p1.pet_img pImg,
  		p1.tag_color tagColor,
  		p1.injection injection,
  		p1.medicine medicine 
  	from member s1 left outer join pet p1 on s1.mno = p1.mno
  	where mno=#{mno}
  </select>
  
  <insert id="insertText" parameterType="diary">
    insert into schedule(pno,startDate,endDate,createDate,content,title,cateCode,dhide) 
    values(#{pno},#{startDate},#{endDate},now(),#{content},#{title},#{dhide})
  </insert>
  
  <delete id="delete" parameterType="int">
    delete from schedule 
  	where dno=#{dno}
  </delete>
  
  <update id="update" parameterType="diary">
    update schedule set 
      startDate=#{startDate},
      endDate=#{endDate},
      title=#{title},
  	  content=#{content}
  	  dhide=#{dhide}
    where dno=#{dno}
  </update>
  
  
</mapper>






